<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <PackageId>Jwelsch.$(AssemblyName)</PackageId>
    <Title>$(AssemblyName)</Title>
    <Authors>Jwelsch</Authors>
    <Company>Justin Welsch</Company>
    <Description>A .NET Standard 2.1 library that contains types used to transform data so it can be consumed by TextTabulator.</Description>
    <Copyright>Copyright 2025-2026 Justin Welsch</Copyright>
    <PackageProjectUrl>https://github.com/jwelsch/TextTabulator/tree/main/src/TextTabulator.Adapters</PackageProjectUrl>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/jwelsch/TextTabulator</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>table;tabulate;text</PackageTags>
    <AssemblyVersion>1.9.2.0</AssemblyVersion>
    <FileVersion>$(AssemblyVersion)</FileVersion>
    <Version>1.9.2</Version>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="$(MSBuildProjectName)Tests" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.CsvHelper" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.Generics" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.Json" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.MLDotNet" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.Reflection" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.Xml" />
    <InternalsVisibleTo Include="TextTabulator.Adapters.YamlDotNet" />
  </ItemGroup>
  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>
</Project>