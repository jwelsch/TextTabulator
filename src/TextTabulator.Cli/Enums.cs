namespace TextTabulator.Cli
{
    public enum DataType
    {
        Csv,
        Json,
        Xml,
        Yaml
    }

    public enum TableStyling
    {
        Ascii,
        Unicode
    }
}
